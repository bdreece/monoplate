import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.bdreece.app',
  appName: 'App',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
